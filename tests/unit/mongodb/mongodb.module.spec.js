var chai = require('chai');
var expect = chai.expect;

var MongoDBModule = require('../../../modules/mongodb/mongodb.modules');

describe('MongoDBModule', () => {
	describe('mongodb.module file', () => {
		it('should test first behavior', () => {});
	});
});
