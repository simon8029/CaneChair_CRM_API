var chai = require('chai');
var expect = chai.expect;

var MongoDBUtil = require('../../../Utils/mongodb/mongodb.util');

describe('MongoDBUtil', () => {
	describe('mongodb.util file', () => {
		it('should test first behavior', () => {});
	});
});
